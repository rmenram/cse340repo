<h1><%= title %></h1>
<%- messages() %>
<div class="login">
    <form action="/login" method="POST">
        <label for="accountEmail">Email:</label>
        <input type="email" id="account_email" name="accountEmail" required>
        <label for="accountPassword">Password:</label>
        <input type="password" id="account_password" name="accountPassword" required pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$">
        <button type="submit">Login</button>
    </form>
    <div>
        <p>Passwords must be:</p>
        <ul>
            <li>12 characters in length, minimum</li>
            <li>contain at least 1 capital letter</li>
            <li>contain at least 1 number</li>
            <li>contain at least 1 special character</li>
        </ul>
    </div>
    <p>No account? <a href="/account/register">Sign-up</a>.</p>
</div>